#import "../template/tables.typ": customizable_tables

= Dicas rápidas

#customizable_tables(
  ([Dicas]),
  (
    ([Congele a linha de cabeçalho: Exibição → Congelar Painéis → Congelar Linha Superior.]),
    ([Use Tabelas (Ctrl + T) para organizar dados — fórmulas se expandem automaticamente.]),
    ([Nomeie intervalos: Fórmulas → Definir Nome. Faz fórmulas como =SOMA(Vendas) mais legíveis.]),
    ([Auto-preenchimento: escreva um padrão nas primeiras células e arraste a alça (canto inferior direito).]),
    ([Preenchimento Relâmpago (Ctrl + E): o Excel detecta o padrão e preenche sozinho (Excel 2013+).]),
    ([Para não perder trabalho, habilite o AutoRecuperação: Arquivo → Opções → Salvar.]),
    ([Validação de Dados: Dados → Validação de Dados — crie listas suspensas para evitar erros.]),
    ([Use comentários (Shift + F2) para deixar anotações nas células sem afetar o conteúdo.]),
    ([Para duplicar uma planilha: clique com o botão direito na aba → Mover ou Copiar → Marcar 'Criar uma cópia'.]),
    ([Proteja a planilha com senha: Revisão → Proteger Planilha — evita edições acidentais]),
  ),
  (yellow),
  (white, rgb(240, 240, 240)),
)